version: "3.9"

services:
  app:
    build:
      context: .
      args:
        API_URL: ${API_URL}
    env_file:
      - .env
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
